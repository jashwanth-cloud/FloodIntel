"use client";
import { Globe2 } from "lucide-react"; import { languages } from "@/lib/languages"; import { useState } from "react";
export default function LanguageSelector(){const [value,setValue]=useState("en");return <label className="flex items-center gap-1 rounded-lg border border-white/10 px-2 py-1.5 text-sm text-slate-200"><Globe2 size={15}/><span className="sr-only">Interface language</span><select aria-label="Interface language" value={value} onChange={e=>{setValue(e.target.value);localStorage.setItem("floodintel-language",e.target.value);window.dispatchEvent(new Event("floodintel-language"));}} className="max-w-24 bg-transparent outline-none">{languages.map(l=><option className="bg-[#061423]" value={l.code} key={l.code}>{l.nativeName}</option>)}</select></label>}
