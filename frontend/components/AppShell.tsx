"use client";
import Link from "next/link"; import { usePathname } from "next/navigation"; import { Menu, X, Waves } from "lucide-react"; import { useState } from "react"; import LanguageSelector from "./LanguageSelector"; import OfflineBanner from "./OfflineBanner";
const nav = [["Home","/"],["Live Map","/map"],["Risk & Forecast","/risk"],["Alerts","/alerts"],["History","/history"],["Report Flood","/report"],["Emergency","/emergency"],["AI Assistant","/assistant"]] as const;
export default function AppShell({children}:{children:React.ReactNode}) { const path=usePathname(); const [open,setOpen]=useState(false); return <><OfflineBanner/><header className="sticky top-0 z-[1000] border-b border-white/10 bg-[#061423]/95 backdrop-blur"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6"><Link href="/" className="flex items-center gap-2 font-bold text-white"><span className="grid h-9 w-9 place-items-center rounded-lg bg-cyan-400 text-[#061423]"><Waves size={21}/></span><span>FloodIntel<span className="hidden text-xs font-normal text-slate-400 sm:inline"> · Flood Intelligence</span></span></Link><nav className="hidden items-center gap-4 xl:flex">{nav.map(([label,href])=><Link key={href} href={href} className={`text-sm ${path===href?"text-cyan-300":"text-slate-300 hover:text-white"}`}>{label}</Link>)}</nav><div className="flex items-center gap-2"><LanguageSelector/><button type="button" onClick={()=>setOpen(!open)} aria-expanded={open} aria-label="Toggle navigation" className="rounded-lg p-2 text-slate-100 hover:bg-white/10 xl:hidden">{open?<X/>:<Menu/>}</button></div></div>{open&&<nav className="border-t border-white/10 px-4 py-3 xl:hidden">{nav.map(([label,href])=><Link onClick={()=>setOpen(false)} key={href} href={href} className={`block rounded-lg px-3 py-2.5 text-sm ${path===href?"bg-cyan-400/10 text-cyan-300":"text-slate-200 hover:bg-white/5"}`}>{label}</Link>)}</nav>}</header><main>{children}</main><footer className="border-t border-white/10 bg-[#061423] py-8 text-center text-xs text-slate-500">FloodIntel · Data shown only when returned by the connected backend.</footer></> }
